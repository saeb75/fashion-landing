import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Account Deletion - AI Personal Assistant",
  description:
    "Learn about deleting your account and data. Official contact information for account deletion requests.",
};

export default function AccountDeletionLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
